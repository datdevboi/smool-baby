type Baby {
  id: ID!
  createdAt: DateTime!
  updatedAt: DateTime!
  name: String!
  dob: DateTime!
  parent: User!
  pictureUrl: String!
}

scalar DateTime

type File {
  filename: String!
  mimetype: String!
  encoding: String!
}

input CreateBabyInput {
  name: String!
  dob: DateTime!
  picture: Upload!
}

type CreateBabyResponse {
  errors: [Error!]
}

type Mutation {
  createBaby(input: CreateBabyInput): Baby
}
